export const meta = {
  name: 'senior-kb-build',
  description: 'Generate 27 senior-depth question banks then audit-and-fix each',
  phases: [
    { title: 'Generate', detail: 'one Sonnet per topic writes a senior-bar bank' },
    { title: 'Audit', detail: 'one Sonnet per topic verifies depth/schema/no-Polish and fixes inline' },
  ],
}

const WT = '/Users/atlasjedi/P/_CLAUDE/major_fucker/.claude/worktrees/senior-kb-rebuild'
const COFORGE = '/Users/atlasjedi/P/_CLAUDE/major_fucker/coforge_cheatsheet.html'
const T = `${WT}/content/topics`

// Each topic: id, prefix, title, sources (absolute paths to MINE/upgrade), focus (must-cover senior items)
const TOPICS = [
  { id: 'java_language', prefix: 'JLNG', title: 'Java Language (8–21)', sources: [`${T}/oop_java_fundamentals.md`],
    focus: 'records; sealed classes + permits; pattern matching for instanceof and switch (preview vs stable per version); switch expressions / yield; text blocks; var scope rules; Optional API and its misuse; Stream API (intermediate vs terminal, laziness, stateful ops, collectors, parallel-stream pitfalls); lambdas and functional interfaces; method references; checked vs unchecked exceptions; try-with-resources; generics (PECS, type erasure, wildcards, bounded types); autoboxing pitfalls; String immutability and interning; equality of wrappers; the Java 8->21 feature timeline (what landed when). Leave equals/hashCode to oop_design.' },
  { id: 'java_collections', prefix: 'JCOL', title: 'Java Collections', sources: [`${T}/oop_java_fundamentals.md`],
    focus: 'THE HashMap deep-dive is mandatory and must be senior/master grade: Java 7 array+linked-list separate chaining vs Java 8 treeify (TREEIFY_THRESHOLD=8 AND MIN_TREEIFY_CAPACITY=64 or it resizes instead, red-black tree, UNTREEIFY_THRESHOLD=6), load factor 0.75, default capacity 16, resize doubling + rehash, hash spread h ^ (h>>>16), index = (n-1) & hash and why capacity is a power of two, complexity O(1) avg / O(n)->O(log n) worst, the mutable-key bug. Also: ArrayList vs LinkedList (cache locality, 1.5x growth); LinkedHashMap (access-order LRU) vs TreeMap (red-black, O(log n)); HashSet/TreeSet; ConcurrentHashMap (Java7 segments vs Java8 CAS + synchronized bins, no null keys/values); List.of / Collections.unmodifiable immutability; fail-fast vs fail-safe iterators (modCount, ConcurrentModificationException); Comparable vs Comparator; equals/hashCode impact on hashed collections.' },
  { id: 'java_concurrency', prefix: 'JCON', title: 'Java Concurrency', sources: [`${T}/oop_java_fundamentals.md`],
    focus: 'thread lifecycle; Runnable/Callable/Future; synchronized (intrinsic monitor lock, reentrancy); volatile (visibility not atomicity); the Java Memory Model and happens-before; atomics + CAS (ABA problem, LongAdder); Lock/ReentrantLock/ReadWriteLock/StampedLock + Condition; ExecutorService and thread pools (core/max/queue/keepAlive, rejection policies, sizing CPU vs IO bound); ThreadPoolExecutor internals; CompletableFuture (thenApply vs thenCompose, exceptionally, allOf/anyOf, supplyAsync executor); ConcurrentHashMap; BlockingQueue; CountDownLatch/CyclicBarrier/Semaphore/Phaser; ThreadLocal and its leaks; deadlock/livelock/starvation; false sharing; virtual threads (Java 21: carrier threads, pinning, when to use); structured concurrency.' },
  { id: 'java_jvm', prefix: 'JVM', title: 'JVM Internals', sources: [`${T}/oop_java_fundamentals.md`],
    focus: 'runtime data areas (heap, JVM stack, metaspace, PC register, native method stack); object layout and header; GC: generational model (eden/survivor/old, minor vs major vs full GC), G1 (regions, pause target), ZGC/Shenandoah (low-pause, colored pointers/load barriers), GC roots and reachability; reference types (strong/soft/weak/phantom + use cases); class loading (loading/linking/initialization, parent-delegation, custom loaders); JIT (C1/C2, tiered compilation, inlining, escape analysis -> scalar replacement / stack allocation, deoptimization); memory leaks (static collections, ThreadLocal, classloader leaks); OOM kinds (Java heap, metaspace, GC overhead limit, direct buffer); key flags (-Xmx/-Xms/-XX:+UseG1GC); diagnostics (jmap/jstack/jstat, heap dump, GC logs).' },
  { id: 'oop_design', prefix: 'OOP', title: 'OOP & Design', sources: [`${T}/oop_java_fundamentals.md`],
    focus: 'four pillars; SOLID (each with a concrete example and a violation); the equals/hashCode contract in full (reflexive/symmetric/transitive/consistent + hashCode consistency, what breaks in HashSet); immutability (how to build a truly immutable class, defensive copies, final caveats); composition vs inheritance (favor composition, LSP, fragile base class); interface vs abstract class (Java 8 default methods); GoF patterns interviewers actually ask (Singleton incl. thread-safe + enum, Factory/Abstract Factory, Builder, Strategy, Observer, Decorator, Adapter, Template Method, Proxy); dependency inversion vs injection; Law of Demeter; cohesion/coupling; DRY/KISS/YAGNI; anemic vs rich domain model; value object vs entity.' },
  { id: 'spring_core', prefix: 'SPRC', title: 'Spring Core (IoC/DI/AOP)', sources: [COFORGE],
    focus: 'IoC and DI (constructor vs setter vs field, why constructor wins); ApplicationContext vs BeanFactory; full bean lifecycle (instantiate -> populate -> *Aware -> BeanPostProcessor before-init -> @PostConstruct/InitializingBean -> BPP after-init -> ready -> @PreDestroy); bean scopes (singleton/prototype/request/session + scoped proxy, prototype-in-singleton trap); @Configuration vs @Component and CGLIB proxying of @Bean methods; JDK dynamic proxy vs CGLIB (interface vs class, final-method limitation); AOP vocabulary (aspect/advice/pointcut/join point) and the proxy self-invocation problem; @Transactional internals (proxy-based, propagation REQUIRED/REQUIRES_NEW/NESTED, isolation, rollback rules incl. checked-exception default, self-invocation + readOnly gotchas); circular dependencies (constructor fails, field/setter resolved via the 3-level cache); @Primary/@Qualifier; @Profile; @Conditional; ApplicationEvents.' },
  { id: 'spring_boot', prefix: 'SPRB', title: 'Spring Boot & Actuator', sources: [COFORGE],
    focus: 'auto-configuration (how @EnableAutoConfiguration + AutoConfiguration.imports + @Conditional* work, ordering, excludes); starters; @SpringBootApplication breakdown; externalized config (property-source precedence, profiles, @ConfigurationProperties vs @Value, relaxed binding). MANDATORY Actuator deep-dive: the full endpoint list (health, info, metrics, env, beans, mappings, loggers, threaddump, heapdump, httpexchanges, conditions, configprops, scheduledtasks) and what each does; only health is exposed over HTTP by default; management.endpoints.web.exposure.include; writing a CUSTOM HealthIndicator (implement Health health()); health GROUPS (management.endpoint.health.group.*); show-details never/when-authorized/always; Kubernetes readiness vs liveness probes (/actuator/health/readiness|liveness) and how they map to startup/rolling deploys; custom metrics via Micrometer; a custom @Endpoint with @ReadOperation. Plus embedded server, layered/fat jar, graceful shutdown.' },
  { id: 'spring_web', prefix: 'SPRW', title: 'Spring Web / REST', sources: [`${T}/rest_api.md`, COFORGE],
    focus: 'DispatcherServlet request lifecycle (handler mapping -> handler adapter -> controller -> message converter / view resolver); @RestController vs @Controller; mapping annotations; @RequestBody/@ResponseBody and HttpMessageConverter (Jackson); @PathVariable/@RequestParam; content negotiation; ResponseEntity; exception handling (@ExceptionHandler, @ControllerAdvice, ResponseStatusException, ProblemDetail RFC 7807); validation (@Valid, JSR-380, BindingResult, group/sequence); filters vs interceptors vs AOP (servlet filter chain ordering, HandlerInterceptor pre/post/afterCompletion); CORS; async MVC (Callable, DeferredResult, WebAsyncTask); ETag/HTTP caching; multipart; brief WebMvc vs WebFlux contrast.' },
  { id: 'spring_data_jpa', prefix: 'SPRD', title: 'Spring Data / JPA / Hibernate', sources: [`${T}/sql.md`, COFORGE],
    focus: 'JPA vs Hibernate vs Spring Data; EntityManager + persistence context (1st-level cache, dirty checking, flush modes); entity states (transient/managed/detached/removed); MANDATORY N+1 problem (cause, how to detect, fixes: JOIN FETCH, @EntityGraph, batch fetch size, DTO projection); LAZY vs EAGER and LazyInitializationException; association mapping (@OneToMany/@ManyToOne/@ManyToMany, owning side, mappedBy, cascade, orphanRemoval); repository hierarchy; derived queries; @Query JPQL vs native, @Modifying; Page vs Slice and the count-query cost; optimistic (@Version) vs pessimistic (LockModeType) locking; identifier generation (IDENTITY vs SEQUENCE vs TABLE and why SEQUENCE batches better); 2nd-level cache; open-session-in-view anti-pattern; batch insert (flush/clear); @Transactional readOnly.' },
  { id: 'spring_security', prefix: 'SPRS', title: 'Spring Security', sources: [COFORGE],
    focus: 'the security filter chain (SecurityFilterChain, filter ordering); authentication vs authorization; AuthenticationManager/Provider/UserDetailsService; SecurityContextHolder (ThreadLocal); password encoding (BCrypt, DelegatingPasswordEncoder); OAuth2/OIDC (authorization-code flow + PKCE, client credentials, resource server vs client); JWT (header.payload.signature, HS256 vs RS256, stateless validation, why you cannot revoke -> short expiry + refresh tokens); method security (@PreAuthorize/@PostAuthorize with SpEL); CSRF (why stateful needs it, why stateless APIs disable it); CORS interplay; stateless session management; Spring Security 6 lambda DSL config; roles vs authorities.' },
  { id: 'sql_postgres', prefix: 'SQL', title: 'SQL & PostgreSQL', sources: [`${T}/sql.md`, COFORGE],
    focus: 'joins (inner/left/right/full/cross/self), GROUP BY/HAVING, subqueries, CTEs (incl. recursive), window functions (ROW_NUMBER/RANK/DENSE_RANK/LAG/LEAD, PARTITION BY); indexes (B-tree, composite + leftmost-prefix rule, covering/INCLUDE, partial, expression, GIN/GiST for JSONB/full-text, BRIN) and when each is used; EXPLAIN/EXPLAIN ANALYZE (seq vs index vs bitmap scan, reading cost, index-only scan); making queries sargable. MANDATORY: transaction isolation levels (read uncommitted/committed/repeatable read/serializable; the anomalies dirty/non-repeatable/phantom; Postgres default is READ COMMITTED and never allows dirty reads); MVCC (xmin/xmax, snapshots, readers do not block writers, VACUUM/autovacuum, table bloat, dead tuples, txid wraparound); locking (FOR UPDATE, advisory locks, deadlocks). Also normalization 1NF-3NF/BCNF + when to denormalize; ACID; connection pooling (pgbouncer); partitioning; JSONB; UPSERT (ON CONFLICT); keyset vs offset pagination.' },
  { id: 'nosql_polyglot', prefix: 'NOSQL', title: 'NoSQL & Polyglot Persistence', sources: [`${T}/allegro_stack.md`],
    focus: 'when NoSQL beats an RDBMS and when it does not; the families (key-value Redis, document Mongo, wide-column Cassandra, graph, search Elasticsearch); CAP theorem (partition tolerance is mandatory in a distributed system, so the real choice is C vs A during a partition) and PACELC; consistency models (strong/eventual/causal/read-your-writes); BASE vs ACID; Cassandra (partition key vs clustering key, model-per-query, tunable consistency / QUORUM, write path commitlog+memtable+SSTable, compaction, no joins); MongoDB (replica sets, sharding, aggregation pipeline); Redis (data structures, persistence RDB vs AOF, eviction policies, cache/lock/rate-limiter, cluster); Elasticsearch (inverted index, analyzers, near-real-time, shards/replicas); polyglot persistence (right store per bounded context: Allegro uses Cassandra/Mongo/Postgres/Elasticsearch/Druid); the dual-write problem.' },
  { id: 'microservices', prefix: 'MS', title: 'Microservices', sources: [`${T}/microservices.md`, COFORGE],
    focus: 'definition and boundaries (bounded context, data-per-service, no shared DB); decomposition (by capability/subdomain); sync (REST/gRPC) vs async (events) and temporal coupling; API gateway + BFF; service discovery (client vs server side, K8s DNS). MANDATORY resilience set: Circuit Breaker (3 states, Resilience4j), Bulkhead (thread-pool vs semaphore isolation — explicitly, this was a gap), Retry with backoff + jitter, Timeout, Rate Limiter, Fallback. MANDATORY data/consistency set: CQRS (command/query split, separate read model, when to use), Event Sourcing (event log as source of truth, replay, snapshots, projections), Saga (choreography vs orchestration, compensating transactions, vs 2PC), Transactional Outbox (the dual-write problem, outbox table + CDC/Debezium relay), idempotency (idempotency keys, at-least-once -> dedupe). Plus distributed tracing (traceId/spanId), eventual consistency, strangler fig, the distributed-monolith anti-pattern, contract testing, service mesh (Envoy sidecar), backpressure.' },
  { id: 'messaging_kafka', prefix: 'KAFKA', title: 'Kafka & Messaging', sources: [`${T}/kafka_hermes.md`],
    focus: 'topics/partitions/offsets; producers (acks 0/1/all, idempotent producer enable.idempotence, transactions for exactly-once, key -> partition and the per-partition ordering guarantee); consumers (consumer groups, partition assignment, rebalancing incl. cooperative/sticky, auto vs manual offset commit, at-most/at-least/exactly-once semantics, consumer lag, max.poll.records and backpressure); retention (time/size) and log compaction; replication (leader/follower, ISR, min.insync.replicas, unclean leader election); DLQ; schema registry/Avro; Kafka vs RabbitMQ (log vs queue); Kafka Streams / KTable basics; Hermes (Allegro’s REST-over-Kafka pub-sub broker abstraction); outbox via Kafka Connect/Debezium.' },
  { id: 'system_design', prefix: 'SD', title: 'System Design', sources: [`${T}/recsys.md`, `${T}/allegro_stack.md`],
    focus: 'a repeatable interview approach (clarify requirements -> capacity estimate -> API -> data model -> high-level design -> deep dive -> bottlenecks); scalability (vertical vs horizontal, statelessness); load balancing (L4 vs L7, algorithms, health checks); caching (client/CDN/app/DB layers, cache-aside vs read/write-through vs write-behind, eviction LRU/LFU, invalidation, thundering herd / cache stampede, Redis); database scaling (read replicas, sharding hash/range/directory, hot partitions, resharding, consistent hashing); CAP tradeoffs; queues for decoupling; rate limiting (token bucket / leaky bucket / sliding window); idempotency; CDN; back-of-the-envelope estimation (QPS, storage, bandwidth); latency numbers to know; p50/p99 tail latency; SLA/SLO/SLI; a worked example on the recommendation serving path (~20k RPS, p99 ~40ms, Faiss ANN); leader election; observability.' },
  { id: 'software_architecture', prefix: 'ARCH', title: 'Software Architecture & DDD', sources: [`${T}/domain_driven_design.md`, `${T}/ecommerce_domain.md`],
    focus: 'architectural styles (layered/n-tier, hexagonal / ports-and-adapters, onion, clean architecture and the dependency rule, event-driven, microkernel/plugin, pipes-and-filters); monolith vs modular monolith vs microservices and when each; DDD strategic design (bounded context, ubiquitous language, context mapping: partnership/customer-supplier/conformist/anti-corruption-layer/shared-kernel/published-language); DDD tactical (entity, value object, aggregate + aggregate root + invariants + transactional boundary, repository, domain service, domain event, factory); CQRS as an architectural pattern; event-driven styles (event notification vs event-carried state transfer vs event sourcing); 12-factor app; ADRs; quality attributes / the -ilities and their tradeoffs; Conway’s law; coupling/cohesion at the service level; the C4 model.' },
  { id: 'resilience_observability', prefix: 'RESIL', title: 'Resilience & Observability', sources: [`${T}/microservices.md`, COFORGE],
    focus: 'Resilience4j modules in depth (CircuitBreaker: count vs time sliding window, failure-rate + slow-call thresholds, state transitions; RateLimiter; Bulkhead semaphore vs thread-pool; Retry with backoff + jitter; TimeLimiter; the order you compose decorators); failing fast; graceful degradation / fallback; timeouts at every hop; retry storms and why jitter matters; idempotency enabling safe retries. Observability three pillars: metrics (Micrometer -> Prometheus, RED and USE methods, histograms vs summaries, cardinality risks), logging (structured/JSON, MDC, correlation/trace IDs, centralized ELK), tracing (OpenTelemetry, spans, W3C traceparent context propagation, sampling). Health checks readiness vs liveness; SLI/SLO/SLA and error budgets; symptom vs cause alerting and alert fatigue; chaos engineering basics.' },
  { id: 'api_design', prefix: 'API', title: 'API Design', sources: [`${T}/rest_api.md`, `${T}/json_xml.md`],
    focus: 'REST principles (resources, HTTP method semantics, the safe/idempotent matrix for GET/POST/PUT/PATCH/DELETE, status codes incl. 201/202/204/400/401/403/404/409/422/429); Richardson maturity model; HATEOAS; versioning (URI vs header vs media-type, deprecation policy); idempotency keys for POST; pagination (offset vs cursor/keyset); filtering/sorting/sparse fieldsets; error format (RFC 7807 ProblemDetail); HTTP caching (ETag, Cache-Control, conditional requests); content negotiation; REST vs gRPC vs GraphQL (when each: gRPC protobuf/HTTP2/streaming; GraphQL over/under-fetching, the N+1 resolver problem, schema); OpenAPI / contract-first; rate limiting with 429 + Retry-After; webhooks; DTO vs entity; request validation.' },
  { id: 'gcp', prefix: 'GCP', title: 'Google Cloud Platform', sources: [COFORGE],
    focus: 'compute options and when to pick each (Compute Engine VMs, GKE managed Kubernetes, Cloud Run serverless containers, App Engine, Cloud Functions); Pub/Sub (topics/subscriptions, push vs pull, at-least-once delivery, ordering keys, dead-letter, vs Kafka); storage (Cloud Storage classes; Cloud SQL managed Postgres/MySQL; Cloud Spanner globally consistent; Firestore; BigQuery serverless columnar OLAP; Bigtable wide-column; Memorystore Redis); IAM (members/roles/policies, least privilege, service accounts, Workload Identity); networking (VPC, global vs regional load balancing, Cloud CDN, Cloud Armor); operations suite (Cloud Logging, Monitoring, Trace, Error Reporting); deployment (Cloud Build, Artifact Registry); Secret Manager; regions/zones; autoscaling; basic cost awareness.' },
  { id: 'containers_k8s', prefix: 'K8S', title: 'Containers & Kubernetes', sources: [`${T}/allegro_stack.md`, COFORGE],
    focus: 'Docker (image vs container, layer caching, multi-stage builds, distroless/slim, .dockerignore, ENTRYPOINT vs CMD, registries, image scanning); Kubernetes core objects (Pod, ReplicaSet, Deployment with rolling update/rollback, Service ClusterIP/NodePort/LoadBalancer, Ingress, ConfigMap, Secret, Namespace, StatefulSet, DaemonSet, Job/CronJob); probes (liveness/readiness/startup — tie explicitly to Spring Boot actuator health groups); resource requests/limits, QoS classes, OOMKilled, CPU throttling; HPA autoscaling; scheduling (nodeSelector/affinity/taints/tolerations); deployment strategies; Helm; persistent volumes/claims; self-healing; 12-factor config via env/ConfigMap.' },
  { id: 'cicd', prefix: 'CICD', title: 'CI/CD', sources: [COFORGE],
    focus: 'CI vs continuous delivery vs continuous deployment; pipeline stages (build -> test -> scan -> package -> deploy); Jenkins (declarative vs scripted Jenkinsfile, agents, stages, shared libraries, credentials); GitHub Actions (workflows/jobs/steps, runners, matrix builds, secrets, caching, reusable workflows); Maven vs Gradle build lifecycle; artifact repositories (Nexus/Artifactory/Artifact Registry); deployment strategies (blue-green, canary, rolling, recreate, feature flags); trunk-based vs gitflow; semantic versioning; test gating and fail-fast; static analysis + coverage gates (SonarQube, JaCoCo); IaC (Terraform); rollback strategy; environment promotion; DORA metrics.' },
  { id: 'testing', prefix: 'TEST', title: 'Testing', sources: [COFORGE],
    focus: 'the test pyramid; JUnit 5 (Jupiter: @Test/@BeforeEach/@ParameterizedTest/@Nested/@DisplayName, assertThrows, extensions vs JUnit4 runners/rules, lifecycle); Mockito (mock vs spy vs stub, when().thenReturn, verify, ArgumentCaptor, @Mock/@InjectMocks, mocking statics, BDDMockito, do not mock value objects); Spring test slices (@SpringBootTest vs @WebMvcTest/@DataJpaTest, MockMvc, @MockBean, WebTestClient, @Transactional rollback in tests); Testcontainers (real Postgres/Kafka in Docker vs H2, and why); test-double taxonomy; AAA / given-when-then; TDD red-green-refactor; coverage (line vs branch, JaCoCo, why coverage is not quality); mutation testing (PIT); consumer-driven contract testing (Pact); flaky tests; what to test vs not.' },
  { id: 'kotlin', prefix: 'KT', title: 'Kotlin', sources: [`${T}/kotlin.md`],
    focus: 'null safety (?, !!, ?., ?:, lateinit, platform types from Java); val vs var; data classes (componentN, copy, generated equals/hashCode); sealed classes/interfaces + exhaustive when; object and companion object; extension functions/properties; higher-order functions and lambdas (it, trailing-lambda convention); scope functions let/run/with/apply/also and when to use each; delegation (by, lazy, observable); inline + reified; collections (immutable vs mutable interfaces, sequences lazy vs eager); smart casts; default and named arguments; destructuring; the type system (Any/Unit/Nothing); declaration-site variance (in/out); Java interop (@JvmStatic/@JvmOverloads/@JvmField, nullability annotations); properties vs fields (backing field, custom accessors). Keep coroutine depth in coroutines_webflux — here just the intro.' },
  { id: 'coroutines_webflux', prefix: 'CORO', title: 'Coroutines & WebFlux', sources: [`${T}/coroutines_webflux.md`],
    focus: 'coroutine fundamentals (suspend functions, continuation-passing-style, the compiler-generated state machine); CoroutineScope/Job/SupervisorJob; structured concurrency; Dispatchers (Default/IO/Main/Unconfined); launch vs async/await; coroutineScope vs supervisorScope; cancellation (cooperative, isActive/ensureActive, CancellationException, NonCancellable); exception handling (CoroutineExceptionHandler, supervision); Flow (cold streams, operators, flowOn, buffer/conflate, backpressure; StateFlow/SharedFlow as hot streams); why GlobalScope is discouraged; Reactor/WebFlux (Mono/Flux, the Reactive Streams spec: publisher/subscriber/subscription/backpressure, operators map/flatMap/zip, schedulers, the never-block rule, event-loop Netty vs thread-per-request MVC); coroutines<->Reactor interop; when reactive pays off (high-concurrency I/O at ~20k RPS); suspend functions in Spring controllers.' },
  { id: 'recsys', prefix: 'REC', title: 'Recommendation Systems', sources: [`${T}/recsys.md`],
    focus: 'PRESERVE the existing strong content but re-tag every question with a [level: ...] tier and add senior/master-tier questions on serving-engineering tradeoffs. Cover: two-stage retrieval vs ranking; embeddings; Two-Tower DLRM (query/item towers, dot product, L2 normalization, weight tying); ANN with Faiss (IVF/HNSW/Flat tradeoffs); the serving path and how to hold p99 ~40ms at ~20k RPS; negative sampling (in-batch, mixed); offline metrics (Recall@K, NDCG) vs online A/B; cold start and content-based encoding; fallback / degraded-mode serving; daily index refresh with blue/green swap.' },
  { id: 'allegro_stack', prefix: 'ALG', title: 'Allegro Stack', sources: [`${T}/allegro_stack.md`],
    focus: 'PRESERVE existing content, re-tag to tiers, add senior-tier questions. Cover: Allegro scale (1000+ microservices on Kubernetes, Envoy service mesh); Hermes (REST-over-Kafka pub-sub); polyglot persistence (Cassandra/Mongo/Postgres/Elasticsearch/Druid and which fits what); Kotlin preferred over Java internally; the recommendations-system team context; ~20k RPS, p99 ~40ms; how a Regular Kotlin dev ships into this stack day one.' },
  { id: 'behavioral_star', prefix: 'STAR', title: 'Behavioral (STAR)', sources: [`${T}/thecodest.md`, COFORGE],
    focus: 'the STAR method (Situation/Task/Action/Result) with model senior-level answers; the common questions (conflict with a colleague, biggest failure, leadership without authority, disagreeing with a manager, tight deadline, a mistake you made, handling ambiguity, learning new tech fast, mentoring, demonstrating ownership); how to scope answers for a SENIOR (impact, tradeoffs, influence, business outcome); the body-leasing / contractor context (The Codest -> client); strong questions to ask the interviewer; rate/scope framing. This bank is reference prep content — model answers are example STAR stories the learner adapts, NOT drill-with-persona.' },
]

const VERIFY_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['topic', 'total_questions', 'tiers', 'polish_found', 'schema_ok', 'fixes_applied', 'concerns'],
  properties: {
    topic: { type: 'string' },
    total_questions: { type: 'integer' },
    tiers: {
      type: 'object', additionalProperties: false,
      required: ['junior', 'regular', 'senior', 'master'],
      properties: {
        junior: { type: 'integer' }, regular: { type: 'integer' },
        senior: { type: 'integer' }, master: { type: 'integer' },
      },
    },
    polish_found: { type: 'boolean' },
    schema_ok: { type: 'boolean' },
    fixes_applied: { type: 'array', items: { type: 'string' } },
    concerns: { type: 'array', items: { type: 'string' } },
  },
}

const genPrompt = (t) => `You are a senior Java/Kotlin backend engineer writing an interview question bank for topic "${t.title}" (id: ${t.id}). The reader is a 5-year backend engineer preparing for SENIOR interviews.

STEP 1 - read the canonical format spec: ${T}/_SCHEMA.md . Follow it byte-for-byte.

STEP 2 - mine and UPGRADE existing source material (do not start from a blank page if sources exist; reuse good questions, translate any Polish to English, deepen shallow answers, fill gaps):
${t.sources.map(s => `  - ${s}`).join('\n')}
These sources may be large. Use Grep to find the relevant sections (search for the topic's keywords) and targeted Read with offset/limit rather than reading 100KB+ files whole. The .html source is a hand-written cheatsheet — mine its tables and code for facts.

STEP 3 - write the bank to ${T}/${t.id}.md following _SCHEMA.md exactly:
- Title line: "# ${t.title} — question bank"
- A "> context" paragraph and a "## Scope" bullet list.
- Question headers: "## Q-${t.prefix}-NNN [bloom: <recall|understand|apply|analyze>] [level: <junior|regular|senior|master>]" with NNN zero-padded sequential from 001.
- Each question has **Question:** / **Model answer:** / **Interview trap:** / **Tags:** , separated by a line with only ---.
- ENGLISH ONLY. No Polish anywhere. Technical terms stay in original form.
- SENIOR BAR: model answers explain internals + operational reality + the gotcha, not just definitions. Include code/tables where they add value.
- Coverage across all four tiers: roughly 6-10 junior, 6-10 regular, 6-10 senior, 4-6 master (total ~28-40). Weight toward regular/senior.

MUST-COVER for this topic (every item below must appear in a question and its model answer):
${t.focus}

Write the complete file with the Write tool. Then reply with ONE line: "<id>: <total> questions (jr/reg/sr/master counts)". Your reply is data, not prose — keep it to that one line.`

const auditPrompt = (t) => `Audit and FIX the question bank at ${T}/${t.id}.md for topic "${t.title}".

1. Read ${T}/_SCHEMA.md then read ${T}/${t.id}.md.
2. Check and FIX (use Edit/Write directly):
   - Schema compliance: header format "## Q-${t.prefix}-NNN [bloom: ...] [level: ...]", all four labelled fields present and in order (**Question:**/**Model answer:**/**Interview trap:**/**Tags:**), --- separators, sequential NNN. Fix any deviation.
   - NO Polish: scan for Polish words (jako, przez, ktory/który, wymaga, uzywa/używa, zamiast, moze/może, byc/być, jest, pytan/pytań, etc.). Translate any to English.
   - All four tiers present with adequate counts (junior/regular/senior/master). If a tier is thin (<4 for jr/reg/sr, <3 master) ADD questions to fill it.
   - Technical correctness, especially these calibration items if relevant to this topic: HashMap treeify (threshold 8 + capacity 64, red-black tree, load factor 0.75); Spring actuator/custom HealthIndicator/health groups; microservices Bulkhead + CQRS + Saga + Outbox. Fix any wrong or shallow answer.
3. Regenerate nothing else. Save fixes in place.
4. Return the structured verdict (the StructuredOutput tool) with accurate post-fix counts.`

phase('Generate')
const results = await pipeline(
  TOPICS,
  (t) => agent(genPrompt(t), { label: `gen:${t.id}`, phase: 'Generate', model: 'sonnet' }),
  (_genText, t) => agent(auditPrompt(t), { label: `audit:${t.id}`, phase: 'Audit', model: 'sonnet', schema: VERIFY_SCHEMA }),
)

const ok = results.filter(Boolean)
log(`Banks built: ${ok.length}/${TOPICS.length}`)
return {
  built: ok.length,
  total: TOPICS.length,
  topics: ok.map(r => ({ topic: r.topic, total: r.total_questions, tiers: r.tiers, polish: r.polish_found, schema_ok: r.schema_ok, concerns: r.concerns })),
}
